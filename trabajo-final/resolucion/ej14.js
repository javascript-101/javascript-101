// 2
const duracion1 = 345;

// 3
const duracion2 = 400;

// 4
const duracion3 = 200;

// 5
console.log( (duracion1 > duracion3) && (duracion3 < duracion2) );

// 6
console.log( (duracion1 < duracion3) && (duracion3 < duracion2) );

// 7
console.log( (duracion1 < duracion3) || (duracion3 < duracion2) );
